source 'https://github.com/CocoaPods/Specs.git'

platform :ios, '14.0'
use_frameworks! :linkage => :static

workspace 'StrangeWords.xcworkspace'

require 'xcodeproj'
require 'pathname'

target 'StrangeWords' do
  pod 'ASCollectionView-SwiftUI', '~> 2.1.1'
end
